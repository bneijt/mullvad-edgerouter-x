{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "stack build",
            "type": "shell",
            "command": "stack build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
